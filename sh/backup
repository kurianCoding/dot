backup(){
	if [[ -z "$1" ]]
	then
		BACK=$(pwd)
		cd $HOME
		nohup tar -cjf ed.tar .vimrc .tmux.conf .tmux .vim >> nohup.out 2>&1 &
		cd $BACK
	else
		nohup tar -cjf $HOME/backup/$1-$(date +%Y-%m-%d).tar $HOME/$1 >> ~/nohup.out 2>&1 &
	fi 
}
